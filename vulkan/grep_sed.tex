\documentclass[a4paper]{moderncv}
\usepackage[scale=0.9]{geometry}
\usepackage{blindtext}
\usepackage{amsmath, kotex}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

\moderncvtheme[blue]{classic}

\firstname{grep -E -rl, grep -E -Hn,}
\familyname{sed -E -i}
\title{for i in \`\,grep -E -rl `pattern'\,\`\,;do sed -E -i `s/pattern/subs/g';done}

\address{Seoul, Korea}
\mobile{010-3201-xxxx}
\email{karng87@gmail.com}
%\photo{logo.png}

\begin{document}
%makelettertitle
%\blindtext
%makeletterclosing
%\newpage

\maketitle

\section{grep}
\cventry{:=}{grep -rl `pattern' ./dir}{recursivly find files with pattern}{}{}{}
\cventry{:=}{grep -nH `pattern' *}{print the line number and a file name}{}{}{}
\cventry{:=}{grep -RnH `pattern' *}{search recursive and print the line number and a file name}{}{}{}
\cventry{:=}{grep -v `pattern' file}{inverse search}{exclusive}{}{}
\cventry{:=}{for i in \`grep -rl `pattern' .dir\`; do grep -nH `pattern' \$ i}{}{}{}{}
\section{sed}
\cventry{:=}{sed -i `s/pattern/subs/g'}{}{}{}{}
\cventry{insert}{for i in `grep -rl `pattern`;do sed -i `s/pattern/subs/g';done}{}{}{}{}
\section{in terminal}
\cventry{}{grep -E -rl `"([\^\,.]+$\backslash$.h)"' . | xargs sed -E -i `s/"([\^\,.]+$\backslash$.h)"/<$\backslash$1>/'}{}{}{}{}
\section{in vim}
\cventry{}{:grep -E -RnH `pattern' *}{recursivly find a file and print file names with a line number in current dir}{}{}{}
\end{document}

